# BetterSpotlight canary profile:
# Force actorized paths as primary for canary reliability gates.
#
# Note: health mode uses "aggregator_primary" (not "actor_primary") in the
# current implementation to select HealthAggregatorActor as the sole source.

export BETTERSPOTLIGHT_CONTROL_PLANE_MODE="actor_primary"
export BETTERSPOTLIGHT_HEALTH_SOURCE_MODE="aggregator_primary"
export BETTERSPOTLIGHT_PIPELINE_ACTOR_MODE="actor_primary"
export BETTERSPOTLIGHT_INFERENCE_SUPERVISOR_MODE="actor_primary"
